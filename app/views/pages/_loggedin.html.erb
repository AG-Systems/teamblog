<% @users.each do | user | %>
<% if user.zipcode ==  current_user.zipcode %>
    <div id="main">
    <div class="card">
      <img class="card-img-top" src="http://placehold.it/350x150" alt="Card image cap">
      <div class="card-block">
        <h4 class="card-title"><%= user.name %> </h4>
        <p class="card-text"><%= user.bio %> </p>
        <%= link_to like_user_path(user), method: :put, class: "btn btn-primary" do %>
        +
        <% end %>
        <%= link_to dislike_user_path(user), method: :put, class: "btn btn-primary" do %>
        -
        <% end %>
      </div>
    </div>
 <% end %>
<% end %>